using UnityEngine;

public class Save : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
        PlayerPrefs.SetInt("WallHealth", 100);
    }

    void LoadData()
    {
    
    }

    void SaveData()
    { 
    
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
